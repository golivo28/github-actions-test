name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "¡Cule vaina bacana!"
      - name: multiline script
        run: |
          python --version
          pip --version
          